<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <title>Tech Blog</title>
</head>

<body>
    <div class="container-fluid">
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand active" href="#">Home</a>
                </div>
                <ul class="nav navbar-nav">
                    {{#if loggedIn}}
                    <li><button id="logoutBtn" class="btn-no-style">Logout</button></li>
                    {{ else}}
                    <li><a href="/login" class="loginBtn">Login</a></li>
                    <script src="/js/login.js"></script>
                    {{/if}}
                    <li><a href="#" class="dashboardBtn">Dashboard</li>
                </ul>
            </div>
            <div class="jumbotron">
                <h1 class="h1">DEV BLOG</h1>
                <p>Join your fellow developers and designers to share thoughts and publications about front end
                    development and UX/UI. </p>
            </div>
        </nav>
    </div>
    <main class="container homepage container-fluid mt-5">
        <!-- Render the contents of the sub layout here -->
        {{{ body }}}
    </main>

    <!-- Render the contents of the sub layout here -->
    {{!-- {{#if logged_in}} --}}
    <script src="/js/logout.js"></script>
    {{!-- {{/if}} --}}
</body>

</html>